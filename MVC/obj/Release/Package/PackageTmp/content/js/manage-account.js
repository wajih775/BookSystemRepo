jQuery(document).ready(function () {

    
   


    //jQuery(".btnDelete").click(function () {
    //    var table = jQuery(this).parent().children("table");
    //    jQuery(table).find("tr").each(function () {
    //        var tr = jQuery(this);

    //        jQuery(tr).find('th').each(function () {
    //            var td = jQuery(this);
    //            if (td[0].cellIndex == td.parent().children().length - 1) {
    //                td.removeClass();
    //            }
    //        });

    //        jQuery(tr).find('td').each(function () {
    //            var td = jQuery(this);
    //            if (td[0].cellIndex == td.parent().children().length - 1) {
    //                td.removeClass();
    //            }
    //        });

    //    });
    //    jQuery(this).text("Cancel Delete");
    //});
    



});
